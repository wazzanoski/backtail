# Shared configuration file for Dockerfile and CI tests
# This file defines all configurable paths and constants that must be consistent
# across the Docker build process and CI testing environment

# SSH configuration file paths
BANNER_FILE="banner.txt"
HOST_KEY_FILE="ssh_host_ed25519_key"
SSHD_CONFIG_FILE="sftp_jail.conf"
